name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: |
            echo "hellow world"
            echo "this is a new line"
      - name: Print goodbye
        run: echo "good bye"
